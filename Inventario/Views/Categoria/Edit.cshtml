@model Inventario.Models.ViewModels.EditCategoriaViewModel
@{
    ViewBag.Title = "Editar Categoría";
}

<h2>@ViewBag.Title</h2>
<div class="row">
    <div class="col-md-12">
        @using (Html.BeginForm("Edit", "Categoria", FormMethod.Post, new { role = "form", @class = "" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(x=>x.Id)
            @Html.ValidationMessage("error", new { @class = "text-danger" })

            @Html.LabelFor(x => x.Nombre)
            @Html.TextBoxFor(x => x.Nombre, new { @class = "form-control" })
            @Html.ValidationMessageFor(x => x.Nombre)

            @Html.LabelFor(x => x.Descripcion)
            @Html.TextBoxFor(x => x.Descripcion, new { @class = "form-control" })
            @Html.ValidationMessageFor(x => x.Descripcion)

            <div style="text-align: right;">
                <input type="submit" value="Guardar" class="btn btn-success" />
            </div>
        }
    </div>
</div>

